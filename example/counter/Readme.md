
Todo:

makesure you have run `npm run build` from `framework/runtime` and folder `dist` exists as well as file `frontend-framework.js` has been created

but before that, also makesure that you have already done the `npm install` on root of this project and on the `framework/runtime` folder

HOW TO RUN:

at the root of this project:

` npm run serve:test` or `npm run serve:examples`

on your browser, direct to example -> counter -> counter.html

