How To Test This?:

from root folder

```sh
# build the framework package
make build

# direct to the example folder
cd example/counter

# install the package
npm install frontend-framework

# run the counter
npm run counter
```