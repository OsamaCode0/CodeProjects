
Todo:

makesure you have run `npm run build` from `framework/runtime` and folder `dist` exists as well as file `frontend-framework.js` has been created

On this example folder
you can create as much as possible examples by creating a new folder for each example...

create your local gitignore inside this example folder

list all your example folder into your .gitignore