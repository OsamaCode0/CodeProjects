


// Create an immutable variable with explicit type annotation
const crewMember1: {
    name: string;
    role: string;
    id: number;
} = {
    name: "John Doe",        // non-empty string for personnel identification
    role: "Pilot",           // non-empty string defining crew responsibilities
    id: 1001                 // positive number for secure database indexing
};